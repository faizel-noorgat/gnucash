# Current State

**Status:** IDLE

**Last Updated:** 2026-09-19T15:34:48Z

**Last Session Summary:** Closed the tenant isolation surface. RLS previously policed only the **36 tables carrying a direct `tenant_id`**; a further **14 reached a tenant only through a parent FK and had no policy at all**, so raw SQL over `accounting_journalline` returned every tenant's ledger lines. Five payload-carrying children were given an explicit `tenant_id` (populated from the parent by `TenantDerivedChildModel`, consistency enforced by a **composite FK** `(parent_id, tenant_id) → parent(guid, tenant_id)` rather than a trigger); nine relationship/sidecar tables got **`EXISTS` policies with matching `WITH CHECK`**. Policies moved from `TO PUBLIC` to **`TO app_user`** — a deliberate reversal of the previous session's reasoning, which is recorded below. `tenants` is now policed on its own primary key. Three defects were found on the way, none of them in the brief: **`TenantContextMiddleware` had never had any effect** (`SET LOCAL` in `process_request` runs in autocommit and is discarded), **`X-Tenant-ID` was trusted with no authorization check**, and **the first draft of `rls/0004` created policies on 14 tables without enabling RLS**. RLS tests **26 → 74**; suite **217 → 265** collected. The 38 failures are unchanged (25 B-0002 + 13 T-0015).

**Branch / HEAD:** `modernization/reimagine-scaffold`. This session's **work commit is `290cc7e73e`** (RLS closure, 18 files, +2330/−61). Commits above it contain only `memory-bank/` files — bookkeeping for this handoff, and their number will vary. To see exactly the tree this session verified, check out `290cc7e73e`.

**Note on this file's history:** record the *work* commit and say explicitly which commit the Memory Bank itself lives in; do not record a bare HEAD sha that the very next commit will invalidate.

**Workstream:** `modernization`

**Build state:** The **reimagined Django tree** was verified this session at work commit `290cc7e73e`, running from `modernized/gnucash-reimagined/`:

```
~/.venvs/gnucash-reimagined/bin/python -m pytest -q
→ 265 collected, 227 passed, 38 failed, on PostgreSQL 16.15
```

Per suite: accounting **56/56**, business_documents **17/17**, reporting **18/18**, identity **52/65**, document_intelligence **7/32**, **rls 74/74** (was 26). The 38 failures are exactly T-0015 (13) plus B-0002 (25) — nothing else. `manage.py check` reports no issues and `makemigrations --check --dry-run` reports no changes.

**Read the caveat before trusting any earlier run log.** An unrelated Claude session was active in this same working tree all session (see F-0008). It ran `git stash push -m wip-b0005-probe` + `git reset --hard HEAD`, discarding ~1600 uncommitted lines (recovered with `git stash apply`; **`stash@{0}` still holds a copy**), and its pytest runs shared the hard-coded `gnucash_test` database, producing ~90 phantom failures — `duplicate key ... auth_permission` after a truncated flush, `database "gnucash_test" does not exist` mid-run, and results that changed between identical invocations. **The numbers above come from a run against a private database** (`gnucash_b0005_test`, via a settings module outside the repo). Any run made earlier in the session on `gnucash_test` is suspect.

Verified from an **empty database**: `migrate` → 51 tables under FORCE RLS → all 57 policies principal-named to `app_user` → 74/74 isolation tests green. Falsified by deleting `ENABLE`/`FORCE` from `rls/0004`: **22 of the new tests fail and none of the original 26 do**, which is direct evidence the pre-existing RLS suite was blind to that bug class.

The **upstream GnuCash C/C++ tree was NOT built or tested this session**. Its last verified result remains the 2026-09-18 preflight: at HEAD `fb2c773bf6` with a clean tree, `ninja` completed 845/845 targets and `ninja check` ran 133 tests with 131 passing; the two failures were `test-qof` and `test-gnc-numeric`, never diagnosed (T-0008).

Environment: dependencies at `~/.venvs/gnucash-reimagined` (Django 5.2.17, pytest 8.4.2, psycopg 3.3.6). **The repo has no venv of its own** — `.venv` is not gitignored. PostgreSQL 16.15 runs locally with `postgres`/`postgres`; **that role is a superuser**, which is load-bearing for the RLS tests. **Docker is not installed**, so `compose.yml` cannot be used and `docker/init-db.sql` never runs.

Test database: `config/settings/test.py` points at `gnucash_test`, created and dropped by pytest. Do not create it by hand — a pre-existing `gnucash_test` makes pytest report "database already exists" and masks the real run. `app_user` is a **cluster-wide** role, not per-database, so it survives test-database teardown.

**Focus:** Closing B-0005 — bringing every tenant-owned table, including the 14 that reach a tenant only through a parent FK, inside the database isolation boundary.

**Next:** **T-0015** — build the identity REST layer (13 tests: `/api/v1/auth/me/`, login, register, tenant list/get/create, membership invite/list, notification list/unread-count). The user deferred it behind the RLS work, which is now committed. **New constraint the RLS closure imposes on it:** `tenants` INSERT now requires `get_current_user_id() IS NOT NULL AND created_by_id = get_current_user_id()`, and the follow-up `Membership` INSERT requires `app.current_tenant_id` — so registration must `SET LOCAL` context to the tenant it just created, in the same transaction, using the id of the row it inserted and never a client-supplied one. See T-0015's `next_action`.

**Also open and unprioritised:** **F-0007** — nothing connects as `app_user`; every environment still uses the `postgres` superuser, so the policies are correct but currently *advisory in practice*. **F-0009** — `practices` and `practice_memberships` remain unpoliced (platform-level, no tenant to key on); the stated residual of this closure.

**Pending Human Action:**

- **F-0008** — a second Claude session is running against this same working tree. It has already discarded uncommitted work once. Decide whether to stop it, and whether to make the test database name derivable so concurrent runs cannot collide.
- **F-0007** — decide whether to split the runtime connection (as `app_user`) from the migration connection (an owning role). `rls.W001` reports the gap on `manage.py check` in the meantime.
- **T-0007** — confirm the reimagine track is the live plan and the same-stack uplift brief is dormant. Both are on disk claiming approval.
- **Q-0001** — confirm whether `borrowed/` plus `contrib/` are the off-limits trees. Proposed by the assistant, never answered.
- **Q-0002** — sign off the five medium-confidence P0 business rules before they are frozen as regression contracts.
- **Q-0004** — decide how a role's permission set narrows to a legal entity. Until this is settled, `get_user_permissions` and the `has_permission` / `user_has_permission` duplication cannot be resolved.

**Known Blockers:**

| ID | Item | Owner | Blocks | Status |
|----|------|-------|--------|--------|
| B-0002 | 25 of 32 document-intelligence acceptance tests subclass `unittest.TestCase` while taking pytest fixtures, so pytest cannot inject them. Structural — no implementation work fixes it. Pre-existing in the legacy tree. | us | criterion 6 | open |

B-0003, B-0004 and **B-0005** are resolved and archived. B-0005's closure is stated with one explicit residual: `practices` / `practice_memberships` stay unpoliced (F-0009), because a Practice is platform-level and has no tenant to key a policy on.

**Decisions passed:**

- 2026-09-19 — **A child's tenancy is materialised or derived, chosen by domain ownership, not applied uniformly.** Five children that carry business/financial payload and are queried directly — `accounting_journalline`, `business_documents_document_line`, `_document_attachment`, `document_extractions`, `document_matches` — get their own `tenant_id` and a plain equality policy. Nine where duplicating it would make the model worse — `role_permissions`, `advisor_access_grants`, `workflow_transitions`, `reporting_dashboardwidget`, `accounting_transactionmetadata`, `_counterpartposting`, `_reconciliationauditlog`, `business_documents_approval_step`, `document_extraction_versions` — get an `EXISTS` policy against their authoritative parent. An `EXISTS` subquery is itself RLS-filtered, so entity narrowing propagates to indirect children for free; only direct-equality children need the entity clause spelled out.
- 2026-09-19 — **Consistency between a child and its parent is a composite foreign key, not a trigger.** `FOREIGN KEY (parent_id, tenant_id) REFERENCES parent (guid, tenant_id)`, requiring a `UNIQUE (guid, tenant_id)` on the parent (free — `guid` is already the PK). A trigger could additionally *fill the column in*, and was rejected: a trigger that silently rewrites a caller-supplied `tenant_id` converts an application bug into a successful write with different data. The key rejects the write instead, at the point of the mistake, and holds against raw SQL the ORM never saw. `ON DELETE CASCADE` mirrors the models' `on_delete`. `TenantDerivedChildModel.derive_tenant_id()` handles the ORM side so ordinary callers never choose a tenant.
- 2026-09-19 — **A child's tenant column mirrors the *shape* of its parent's, not a stricter one.** `JournalLine`, `DocumentLine` and `DocumentAttachment` take a real FK to `Tenant` because their parents do; `DocumentExtraction` and `DocumentMatch` take a bare `UUIDField` because `Document.tenant_id` is a bare `UUIDField`. A child typed more strictly than its parent is not safer — it makes rows the parent accepts impossible to attach anything to. (Found by a passing test breaking: a FK from `document_extractions` to `tenants` rejected an extraction whose document was created against a fabricated UUID.)
- 2026-09-19 — **RLS policies are `TO app_user`, reversing the previous session's `TO PUBLIC` decision.** The earlier reasoning — that naming a role leaves the policy inapplicable to others, and default-deny is an outage rather than a leak — is still true and is still the wrong trade. `PUBLIC` makes the policy a statement about nobody in particular and silently extends it to every role ever granted privileges on the table. Least privilege and default deny mean naming the role. Implemented with `ALTER POLICY ... TO app_user` rather than DROP/CREATE, so the predicates written and reviewed in `0002` are not restated and cannot drift.
- 2026-09-19 — **The pre-context read set, and check-before-set ordering.** `AuthorizationService.can_access_tenant()` reads `memberships`, `advisor_access_grants`, `client_engagements` and `tenants` — the very tables RLS confines — so with no tenant context it returns `False` for everyone, including legitimate members. Four permissive `FOR SELECT` policies grant a caller their own rows: `memberships.user_id = get_current_user_id()`, `tenants` via a membership of theirs, `advisor_access_grants.practice_user_id = get_current_user_id()`, `client_engagements` via a practice membership of theirs. Permissive policies are OR'd, so these widen SELECT only; writes still require context. The middleware then follows the order the user asked for — authenticate → requested tenant → `can_access_tenant()` → establish context — and a middleware bug yields *no* context and zero rows, i.e. it fails closed. The advisor variant is deliberately **weaker** than the authorization rule (it ignores grant status, revocation and expiry): it answers "which tenants may you ask about", not "which may you use", and a second copy of the rule that drifts would lock out a legitimate advisor.
- 2026-09-19 — **`TenantContextMiddleware` owns a `transaction.atomic()` around `get_response`.** It cannot be a `MiddlewareMixin`: `process_request` runs before the view and in autocommit, so every `SET LOCAL` it issues is discarded — confirmed directly against PostgreSQL (`WARNING: SET LOCAL can only be used in transaction blocks`). The cost is real and accepted: every request holds a transaction open for its duration, and a `StreamingHttpResponse` outlives the block carrying the context. There is no way to have transaction-local RLS context without a transaction spanning the work that uses it. The authorization decision is **not** gated on `RLS_ENABLED` — that setting switches off the database mechanism, and using it to skip the check would make every non-RLS environment accept any tenant id from any authenticated caller.
- 2026-09-19 — **`tenants` is policed, keyed on its own primary key (`guid`).** SELECT is permitted for the current tenant or any tenant the caller holds a membership in; UPDATE/DELETE for the current tenant only; INSERT has a bootstrap carve-out requiring an authenticated user who records themselves as `created_by_id`, because registration creates a tenant before any membership can authorize reading it back. That carve-out is the whole bootstrap allowance and grants nothing but the ability to create the row.
- 2026-09-19 — **`roles` exempts system roles explicitly:** `tenant_id = get_current_tenant_id() OR (tenant_id IS NULL AND is_system_role)`. A bare equality would make every system role invisible and silently remove the permissions it grants, because `AuthorizationService.has_permission` explicitly matches `Q(tenant=tenant) | Q(tenant__isnull=True)`. The `is_system_role` half keeps an accidental NULL from becoming globally visible. `role_permissions` inherits the same rule through its `EXISTS`.
- 2026-09-19 — **`accounting/0004` disables the ADR-010 line trigger for its backfill, and re-enables it.** The trigger compares the whole row (`to_jsonb(OLD)` minus an allow-list), so the `UPDATE` setting `tenant_id` on lines of *posted* entries would raise `ADR-010: line ... is immutable`. The alternative — amending the trigger to permit a NULL → value transition on `tenant_id` — was rejected as leaving a permanent exception in an immutability guard for the benefit of a one-off migration. The migration is atomic, so a failure between the disable and the re-enable rolls both back; `test_the_posted_immutability_trigger_survived_the_tenant_backfill` asserts the trigger is enabled afterwards rather than trusting the comment.
- 2026-09-19 — **`rls.W001` warns, rather than errors, when the connection can bypass RLS.** Every environment here connects as the `postgres` superuser, so the policies are currently advisory in practice (F-0007). Severity is a warning because migrations genuinely need the owning role and there is a single `DATABASES['default']`; an error would block `manage.py migrate` on exactly the deployments that need to run it. Splitting the runtime and deploy connections is a deployment decision, not one a check should force.
- 2026-09-19 — **Creating a policy on a table whose RLS is not enabled is a silent no-op**, because PostgreSQL only consults policies once `relrowsecurity` is set. The first draft of `rls/0004` shipped exactly that on 14 tables, with every other assertion still passing. Every newly policed table now goes through `_enable_and_policy()`, and `test_a_policy_alone_does_not_count_as_isolation` asserts the `relforcerowsecurity` flag rather than the mere existence of a policy.
- 2026-09-19 — **Posting is one atomic domain operation.** `DocumentPostingService.post_document()` owns the complete lifecycle: lock → validate → post → mark posted → persist the journal reference → audit → commit once. The legacy two-step contract (`post_document()` then a caller-side `mark_posted()`) is deliberately **not** preserved, because it created a consistency window in which a `JournalEntry` was posted while the `AccountingDocument` remained DRAFT. This is an intentional improvement over legacy behaviour, not a compatibility regression. Callers must no longer mark the document posted themselves. (Resolves Q-0003.)
- 2026-09-19 — `AdvisorAccessGrant.revoke()`'s parameter is `revoked_by`, matching the field it sets and the BR-PRACTICE-002 acceptance contract. `ApiToken.revoke()` still takes `revoked_by_user` — tracked as F-0006. **Re-confirmed: kept as-is, both call sites green.**
- 2026-09-19 — `DocumentLine.project` stays **removed**; do NOT restore or invent `accounting.Project`. Project / cost-centre / department / location tracking is intended to arrive as one generalised **Dimension** model, designed deliberately, not pre-empted by a bespoke Project. (F-0003)
- 2026-09-19 — Tenant context is centralised in `common/middleware/tenant.py` as the **single authoritative implementation**, extended with `TenantContext`, `tenant_context()`, `TenantContextManager` and `get_current_tenant_id` / `get_current_user_id` / `get_current_entity_id`. Apps may re-export it (reporting's `middleware.py` is a shim only) but must not reimplement tenancy. `identity_access.infrastructure.tenant_context` is not restored. **`TenantContext` and `TenantContextManager` perform no authorization check of their own** — they are the internal entry points for code that has already established a tenant by business means, and must not be reached from request handling with a caller-supplied tenant.
- 2026-09-19 — Posted-journal immutability is enforced in PostgreSQL (`accounting/0003_adr010_posted_immutability.py`), not only in the ORM. The policy is an **allow-list of mutable columns** compared via whole-row `to_jsonb(OLD)` minus those keys, so columns added later are protected by default. Only `updated_at` is mutable on a posted entry; `updated_at`, `reconcile_status`, `date_reconciled` and `online_id` on a posted line. There is **no bypass of any kind** and there must never be one.
- 2026-09-19 — BR-BUS-002 (invoice amounts stored positive) is enforced by DB `CheckConstraint`s on `DocumentLine` (`quantity > 0`, `unit_price >= 0`), **not** field validators, because `DocumentLine.save()` does not call `full_clean()`. Revisitable.
- 2026-09-19 — The modernization track is gated on the accounting golden suite: golden must be green before dependent business-document posting work proceeds. It is green (40/40 golden, 56/56 accounting on PostgreSQL).
- 2026-09-19 — An unconditional `pytest.xfail(...)` inside a test body is treated as a defect, not a skip: it silently disables every assertion below it. Two such calls were removed from `tests/business_documents/test_acceptance.py`, which is what exposed that `DocumentPostingService` was broken.
- 2026-09-19 — The four working trees (`.claude/`, `analysis/`, `memory-bank/core/`, `modernized/`) are **committed to the fork**, not excluded from git. Committed on branch `modernization/reimagine-scaffold`, so `stable` stays clean against upstream. `memory-bank/runtime/` live state, `.claude/scheduled_tasks.lock`, and `__pycache__`/`*.pyc` stay ignored. (Resolves D-0001.)
- 2026-09-19 — Session memory ported with two skills (`session-start`, `session-close`) rather than one multi-mode skill, matching the two Cursor commands 1:1.
- 2026-09-19 — Tracking axes adapted from py-hoarder's SDLC-phase × project-plan model to gnucash-native axes (workstream + build state).
- 2026-09-19 — `prompts/` and `docs/40-delivery/` deliberately excluded from the port; lessons learned live in `memory-bank/core/lessons-learned.md` instead.
- 2026-09-19 — Scratchpad tests ported from pytest to stdlib `unittest` so the suite adds no dependency to an upstream C/C++ project.
- 2026-09-18 — Forked to `faizel-noorgat/gnucash` (`origin`) with `Gnucash/gnucash` as `upstream`; `stable` was 641 ahead / 40 behind, so the fork was reset to `upstream/stable` and force-pushed. The pre-reset divergence came from unrelated history in the fork, not from this work.
- 2026-09-18 — Analysis scope: `bindings/*.i` counts as source (SWIG input), not generated code. Off-limits was proposed as `borrowed/` + `contrib/` — still unconfirmed (Q-0001).
- 2026-09-19 — Modernization target is a **greenfield multi-tenant cloud accounting SaaS**, not a same-stack uplift: Django + PostgreSQL + Celery + React/TypeScript, five bounded contexts (`identity`, `accounting`, `business_documents`, `document_intelligence`, `reporting`), delivered as a modular monolith with one Django app per context and REST only.
- 2026-09-19 — Rejected at design review: GraphQL, Django Channels/WebSockets, a separate AI service container, eight bounded contexts, full intercompany consolidation, and putting many client companies into one Tenant. A practice managing many clients is modelled explicitly (`Practice`, `PracticeMembership`, `ClientEngagement`, `AdvisorAccessGrant`), never by conflating tenants.
- 2026-09-19 — Multi-tenancy is shared-schema PostgreSQL with row-level security (`SET LOCAL`, transaction-local, fail-closed), not schema-per-tenant. **Provisioned, and as of this session closed over every tenant-owned table.**
- 2026-09-19 — GnuCash's dual `amount` (account commodity) / `value` (transaction currency) fields are preserved, with trading accounts kept internally but hidden from the UI.
