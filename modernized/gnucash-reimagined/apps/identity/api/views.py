"""
Identity API Views

Stub view module for the Identity & Access API.
Implement DRF viewsets and views here as endpoints are built out.
"""

# from rest_framework import viewsets, permissions
# from apps.identity.models import User, Tenant
# from .serializers import UserSerializer, TenantSerializer
#
#
# class UserViewSet(viewsets.ModelViewSet):
#     """CRUD + list for platform users."""
#
#     queryset = User.objects.all()
#     serializer_class = UserSerializer
#     permission_classes = [permissions.IsAuthenticated]
#
#
# class TenantViewSet(viewsets.ModelViewSet):
#     """CRUD + list for tenants (SME customer organizations)."""
#
#     queryset = Tenant.objects.all()
#     serializer_class = TenantSerializer
#     permission_classes = [permissions.IsAuthenticated]
